package com.users.mvc.service;


import com.users.dtos.ApiResponse;
import com.users.dtos.AuthResponse;
import com.users.dtos.UserDTO;
import com.users.entity.User;
import com.users.jwt.UserDetailsImpl;

public interface AuthService  {

	UserDetailsImpl register(UserDTO user);

    UserDetailsImpl login(String username, String password);
    
     UserDetailsImpl registerOuth(User user);
}